# Node Docker image Builder(NDB)

## How to
- cd project directory
- execute script build_image.sh 
## script args
image_name [entry_path] [PORT] [add_file1] [file2] ... [fileN]

for example: 
some_dir/../build_image.sh myimage app/index.js 3000 app
